version: '3.1'

services:
  ghost:
    image: ghost:5-alpine
    restart: always
    ports:
      - 2368:2368
    environment:
      # URL changed to subdomain to avoid conflict with your main app on pry.hu
      url: https://ghost.pry.hu
      
      database__client: mysql
      database__connection__host: db
      database__connection__user: root
      database__connection__password: root_password
      database__connection__database: ghost
      
      # Mail Configuration
      # mail__transport: SMTP
      # mail__options__service: Mailgun
      # mail__options__auth__user: postmaster@your-domain.com
      # mail__options__auth__pass: your-mailgun-password
    volumes:
      - ghost_content:/var/lib/ghost/content
    depends_on:
      - db

  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root_password
    volumes:
      - db_data:/var/lib/mysql

volumes:
  ghost_content:
  db_data: